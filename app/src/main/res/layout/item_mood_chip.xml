<?xml version="1.0" encoding="utf-8"?>
<!--
    Mood / genre chip  —  wrap_content width, 36 dp height
    Used by MoodChipAdapter in the Moods & Genres row.
    Background: dark pill with accent stroke (defined in drawable/bg_mood_chip.xml).
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="36dp"
    android:layout_marginEnd="8dp"
    android:background="@drawable/bg_mood_chip"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackgroundBorderless">

    <TextView
        android:id="@+id/moodChipText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:maxLines="1"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:fontFamily="sans-serif-medium" />

</FrameLayout>
